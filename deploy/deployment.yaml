apiVersion: apps/v1
kind: Deployment
metadata:
  name: spam-agent
  namespace: spam-agent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: spam-agent
  template:
    metadata:
      labels:
        app: spam-agent
    spec:
      serviceAccountName: spam-agent
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: spam-agent
          image: git.torden.tech/jonasbg/spam-agent:latest
          imagePullPolicy: Always
          env:
            - name: CLUSTER_NAME
              value: "talos-proxmox-cluster"
            - name: GOGC
              value: "50"
            - name: GOMEMLIMIT
              value: "96MiB"
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
