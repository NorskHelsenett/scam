apiVersion: v1
kind: Namespace
metadata:
  name: scam
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: scam
  namespace: scam
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: scam
rules:
  - apiGroups: [""]
    resources: ["nodes", "pods", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses", "ingressclasses"]
    verbs: ["get", "list", "watch"]
  # Permissions for Gateway API resources are declared proactively. If the
  # CRDs aren't installed, the agent's discovery probe skips the watchers
  # and these rules are harmless.
  - apiGroups: ["gateway.networking.k8s.io"]
    resources:
      - gateways
      - gatewayclasses
      - httproutes
      - grpcroutes
      - tlsroutes
      - tcproutes
    verbs: ["get", "list", "watch"]
  # Traefik's own CRDs for HTTP/TCP/UDP routing. The agent probes for the
  # current "traefik.io" and legacy "traefik.containo.us" groups.
  - apiGroups: ["traefik.io", "traefik.containo.us"]
    resources:
      - ingressroutes
      - ingressroutetcps
      - ingressrouteudps
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: scam
subjects:
  - kind: ServiceAccount
    name: scam
    namespace: scam
roleRef:
  kind: ClusterRole
  name: scam
  apiGroup: rbac.authorization.k8s.io
